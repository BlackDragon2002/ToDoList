<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/action_search"
        android:icon="@drawable/ic_search"
        android:title="Search"
        app:actionViewClass="androidx.appcompat.widget.SearchView"
        app:showAsAction="always|collapseActionView"/>

    <item
        android:icon="@drawable/ic_sort"
        android:title="Sort"
        app:showAsAction="always">

        <menu>
            <item
                android:id="@+id/action_sortByName"
                android:title="Sort by name"/>

            <item
                android:id="@+id/action_sortByDate"
                android:title="Sort by date"/>
        </menu>
    </item>

    <item
        android:id="@+id/action_hideCompletedTask"
        android:checkable="true"
        android:title="Task Completed"
        app:showAsAction="never"/>

    <item
        android:id="@+id/action_deleteCompletedTask"
        android:title="Delete all completed"
        app:showAsAction="never"/>

</menu>